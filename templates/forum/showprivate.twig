{% extends "_layout.twig" %}

{% if pmsg.title %}
	{% set title = pmsg.title %}
{% else %}
	{% set title = '(untitled)' %}
{% endif %}
{% set noindex = true %}

	{% block content %}
<h1>{{ title }}</h1>
{{ render_page_bar(pagebar) }}

{{ threadpost(pmsg) }}

{{ render_page_bar(pagebar) }}
	{% endblock %}