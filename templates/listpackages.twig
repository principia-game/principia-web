{% extends "_layout.twig" %}

{% set title = "Packages" %}

	{% block content %}
<h1>Packages</h1>

<div class="level-list">
{% for pkg in packages %}
	{{ package(pkg) }}
{% else %}
	No packages.
{% endfor %}
</div>

{{ pagination(package_count, glob_lpp, 'packages?page=%s', page) }}

<br><br>
	{% endblock %}